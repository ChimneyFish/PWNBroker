data/scanner.db*
data/secret_key.txt
data/encryption_key.txt
data/logs/
data/ssl/
evidence_uploads/
venv/
logs/
.env
__pycache__/
*.pyc
scripts/build/tailwindcss

# PEN is cloned+built by setup.sh at deploy time (tools/pen), not vendored
tools/pen/

# Built out-of-band (GitHub Actions or a local Windows build — see
# installer/windows/) and placed here manually; not tracked in this repo.
app/static/agent/PwnBrokerAgentSetup.exe

# PyInstaller/Inno Setup build output (installer/windows/build.bat runs
# from the repo root, so these land here, not under installer/windows/).
/build/
/dist/
